# NecoRAG 智能路由与策略融合引擎设计文档

## 📋 概述

本文档详细描述 NecoRAG v1.8-Alpha 版本新增的**智能路由与策略融合引擎**(Intelligent Routing & Strategy Fusion Engine),该引擎整合了意图分析、CoT思维链推理和用户画像三大核心能力，构建了智能化的检索 - 响应决策系统。

**文件位置:** `/Users/ll/NecoRAG/design/SMART_ROUTING_FUSION_ENGINE.md`

---

## 🎯 核心价值

### 解决的问题

在传统 RAG 系统中:
1. **策略单一化**: 所有查询使用相同的检索策略，忽略问题类型的多样性
2. **缺乏个性化**: 不考虑用户的专业背景和偏好，采用"一刀切"的响应方式
3. **资源浪费**: 简单问题和复杂问题消耗相同的计算资源，效率低下
4. **体验不佳**: 专家用户觉得啰嗦，新手用户觉得难懂

### NecoRAG 的解决方案

通过**三层决策架构**实现智能化适配:

```
┌─────────────────────────────────────────────────────────────┐
│                  智能路由与策略融合引擎                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   第一层：意图识别层 ──▶ 识别问题类型和复杂度                │
│                                                             │
│   第二层：用户画像层 ──▶ 匹配用户专业度和偏好               │
│                                                             │
│   第三层：策略融合层 ──▶ 动态选择最优策略组合               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 核心功能模块

### 1. 意图驱动的初始路由 (Intent-Driven Initial Routing)

基于 7 大类语义意图，自动路由到对应的策略模板:

| 意图类型 | 默认策略组合 | CoT 触发概率 | 典型响应模式 |
|---------|-------------|-------------|-------------|
| **事实查询** | 精确向量匹配 (0.7) + 关键字检索 (0.3) | 低 (0.1) | 直接回答 + 来源引用 |
| **比较分析** | 多实体并行检索 (0.5) + 图谱关联 (0.3) + 对比生成 (0.2) | 中 (0.4) | 对比表格 + 优劣势分析 |
| **推理演绎** | 图谱多跳检索 (0.4) + HyDE 增强 (0.3) + CoT 推理 (0.3) | 高 (0.9) | 思维链展示 + 逻辑推导 |
| **概念解释** | 语义检索 (0.6) + 层级上下文 (0.3) + 示例生成 (0.1) | 中 (0.5) | 定义 + 类比 + 实例 |
| **摘要总结** | 广泛检索 (0.5) + 聚合排序 (0.3) + 要点提炼 (0.2) | 中 (0.3) | 核心观点列表 + 结构化摘要 |
| **操作指导** | 程序记忆检索 (0.6) + 时序排列 (0.3) + 步骤验证 (0.1) | 低 (0.2) | 分步指南 + 注意事项 |
| **探索发散** | 扩散激活 (0.4) + 新颖性优先 (0.4) + 跨领域联想 (0.2) | 高 (0.7) | 多角度探索 + 创新联想 |

**策略权重动态调整公式:**

```math
strategy\_weight_i = base\_weight_i \times \alpha_{intent} \times \beta_{user} \times \gamma_{context}
```

其中:
- `base_weight_i`: 意图类型对应的基础权重
- `α_intent`: 意图置信度调节因子 (置信度越高，越倾向默认策略)
- `β_user`: 用户偏好调节因子 (基于用户历史满意度)
- `γ_context`: 上下文调节因子 (会话历史、时间压力等)

### 2. 用户画像增强的个性化适配 (User Profile-Enhanced Personalization)

基于用户的个人工作空间数据，进行个性化定制:

#### 专业度适配矩阵

| 用户专业度 | 检索策略调整 | 响应风格调整 | CoT 展示策略 |
|-----------|-------------|-------------|-------------|
| **专家 (Expert, ≥0.8)** | 增加深度检索权重，减少基础概念检索 | 简洁、专业术语、假设已知背景 | 仅展示关键推理跳跃，省略基础步骤 |
| **中级 (Intermediate, 0.5-0.8)** | 平衡深度和广度 | 适度解释专业术语，提供背景链接 | 完整展示推理链，标注关键节点 |
| **新手 (Novice, <0.5)** | 增加基础概念和示例检索 | 通俗易懂、类比解释、逐步引导 | 详细展示每一步，提供额外说明 |

#### 风格偏好适配

```python
class UserStylePreference:
    detail_level: Literal["concise", "balanced", "comprehensive"]
    tone: Literal["formal", "casual", "humorous"]
    format_preference: Literal["text", "bullet_points", "table", "diagram"]
    citation_style: Literal["inline", "footnote", "endnote", "none"]
    example_preference: Literal["many", "moderate", "few", "none"]
```

#### 个性化响应示例

**问题:** "微服务的容错机制如何实现？"

**专家用户 (简洁专业型):**
> 主要通过熔断器模式 (Circuit Breaker)、舱壁隔离 (Bulkhead) 和降级策略。推荐 Hystrix/Resilience4j，参考 [Netflix TechBlog]。

**中级用户 (平衡解释型):**
> 微服务容错的核心是故障隔离和快速失败:
> 1. **熔断器模式**: 检测失败次数，超过阈值自动熔断...
> 2. **舱壁隔离**: 将资源分组隔离，避免级联故障...
> 3. **降级策略**: 服务不可用时提供备用方案...

**新手用户 (详细引导型):**
> 想象一个电力系统：当某个电器短路时，保险丝会熔断，保护整个电路不被烧毁。微服务的容错机制类似...
> [配合电路类比图]...

### 3. CoT 思维链的智能触发与深度调节 (Adaptive CoT Triggering & Depth Control)

#### CoT 触发决策树

```
查询输入
    │
    ▼
┌─────────────────┐
│ 意图分类器       │
└────┬────────────┘
     │
     ├─▶ 推理演绎类 ──▶ 强制触发 CoT (置信度>0.8)
     │                   │
     │                   ▼
     │              ┌────────────┐
     │              │ 用户画像检查 │
     │              └────┬───────┘
     │                   │
     │              ┌────┴────┐
     │              │         │
     │          专家用户   新手用户
     │              │         │
     │          精简 CoT   详细 CoT
     │          (2-3 步)    (5-7 步)
     │
     ├─▶ 事实查询类 ──▶ 默认不触发
     │                   │
     │                   ▼
     │              例外情况:
     │              - 新手用户明确要求"解释为什么"
     │              - 低置信度结果 (<0.6)
     │              - 跨领域概念首次出现
     │
     └─▶ 比较分析类 ──▶ 条件触发
                        │
                        ▼
                   复杂度评估
                   (实体数>3 或关系>5)
```

#### CoT 深度分级

| 深度等级 | 推理步数 | 适用场景 | 展示内容 |
|---------|---------|---------|---------|
| **L1 - 精简版** | 1-2 步 | 专家用户、简单问题 | 关键推理跳跃，省略中间步骤 |
| **L2 - 标准版** | 3-4 步 | 中级用户、中等复杂度 | 完整逻辑链，标注关键节点 |
| **L3 - 详细版** | 5-7 步 | 新手用户、复杂推理 | 每步详细说明，附带示例和类比 |
| **L4 - 探索版** | 7+ 步 | 探索性问题、跨领域整合 | 多路径推理，展示不同视角 |

#### 动态深度调节算法

```python
def determine_cot_depth(query, user_profile, intent_result):
    """动态确定 CoT 推理深度"""
    base_depth = 3  # 默认 3 步
    
    # 意图复杂度调节
    if intent_result.complexity >= 0.8:
        base_depth += 2
    elif intent_result.complexity <= 0.4:
        base_depth -= 1
    
    # 用户专业度调节
    expertise = user_profile.expertise_level.get(intent_result.domain, 0.5)
    if expertise >= 0.8:
        base_depth -= 1  # 专家减少步骤
    elif expertise <= 0.3:
        base_depth += 2  # 新手增加步骤
    
    # 用户显式偏好
    if user_profile.preference.get("cot_detail") == "minimal":
        base_depth -= 1
    elif user_profile.preference.get("cot_detail") == "maximal":
        base_depth += 2
    
    # 上下文调节 (会话历史)
    if _user_asked_followup(query):
        base_depth -= 1  # 追问时简化
    
    return max(1, min(base_depth, 7))  # 限制在 1-7 步
```

### 4. 多策略并行与融合 (Multi-Strategy Parallelization & Fusion)

对于复杂查询或低置信度场景，采用多策略并行检索，然后融合结果:

#### 并行检索架构

```
                    用户查询
                      │
                      ▼
            ┌─────────────────┐
            │ 意图识别 + 画像  │
            └────────┬────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
   向量检索      图谱多跳      HyDE 增强
   (Qdrant)     (Neo4j)      (假设答案)
        │            │            │
        └────────────┴────────────┘
                     │
                     ▼
            ┌─────────────────┐
            │   结果融合层     │
            │  - 去重          │
            │  - 评分归一化    │
            │  - 多样性保证    │
            └────────┬────────┘
                     │
                     ▼
            ┌─────────────────┐
            │   重排序层       │
            │  BGE-Reranker   │
            │  + Novelty 检测  │
            └────────┬────────┘
                     │
                     ▼
               最终 Top-K 结果
```

#### 融合评分公式

```math
fusion\_score(d) = \sum_{s \in Strategies} w_s \cdot norm(score_{s,d}) \cdot (1 + novelty_d) \cdot diversity_penalty
```

其中:
- `w_s`: 策略 s 的权重 (由意图和用户画像决定)
- `norm(score_{s,d})`: 文档 d 在策略 s 下的归一化分数
- `novelty_d`: 文档 d 的新颖性得分 (基于 n-gram 相似度)
- `diversity_penalty`: 多样性惩罚项 (避免结果过于集中)

#### 多样性控制参数

```python
diversity_config = {
    "max_same_domain_ratio": 0.6,      # 同一领域结果最多占 60%
    "min_cross_domain_count": 2,       # 至少包含 2 个跨领域结果
    "temporal_diversity": True,        # 混合新旧知识
    "source_diversity": True           # 避免单一来源垄断
}
```

### 5. 早停与降级机制 (Early Termination & Degradation)

为平衡效果和延迟，实现智能的早停和降级决策:

#### 早停条件 (满足任一即停止)

1. **置信度阈值达成**: `best_result.confidence >= 0.95`
2. **边际收益递减**: `additional_search_improvement < 0.02`
3. **资源约束**: `elapsed_time > max_allowed_latency * 0.8`
4. **用户满意度预测**: `predicted_satisfaction >= 4.5`

#### 降级策略 (从高到低)

| 降级等级 | 触发条件 | 降级动作 | 效果影响 |
|---------|---------|---------|---------|
| **Level 1** | 延迟>500ms | 减少并行策略数 (保留最优 2 个) | 轻微 |
| **Level 2** | 延迟>700ms | 跳过 CoT，使用直接回答 | 中等 |
| **Level 3** | 延迟>900ms | 仅执行向量检索，跳过图谱多跳 | 显著 |
| **Level 4** | 延迟>1000ms | 返回缓存结果或简化答案 | 较大 |

### 6. 实时反馈闭环 (Real-time Feedback Loop)

每次交互后收集显式和隐式反馈，用于优化未来的路由决策:

#### 反馈信号类型

| 信号类型 | 采集方式 | 权重 | 更新频率 |
|---------|---------|------|---------|
| **显式评分** | 用户点赞/点踩 | 1.0 | 实时 |
| **查询改写** | 用户修改原查询 | 0.8 | 实时 |
| **会话放弃** | 用户提前结束会话 | 0.7 | 实时 |
| **二次检索** | 短时间内再次搜索 | 0.6 | 实时 |
| **停留时长** | 在结果页停留时间 | 0.5 | 批量 |
| **引用行为** | 用户复制/分享结果 | 0.9 | 实时 |

#### 策略权重更新规则 (在线学习)

```python
def update_strategy_weights(intent_type, strategy_id, feedback_signal):
    """基于反馈更新策略权重"""
    old_weight = strategy_weights[intent_type][strategy_id]
    reward = feedback_signal.normalized_score()  # [-1, 1]
    
    # 增量更新 (类似 MAB 算法)
    new_weight = old_weight + learning_rate * (reward - expected_reward)
    
    # 平滑限制
    new_weight = clip(new_weight, min=0.1, max=2.0)
    
    strategy_weights[intent_type][strategy_id] = new_weight
```

#### 用户画像实时更新

```python
class UserProfileUpdater:
    def on_feedback(self, user_id, query, response, feedback):
        user = self.user_store.get(user_id)
        
        # 更新领域专业度评估
        if feedback.is_positive():
            user.expertise_in(query.domain) += 0.02
        else:
            user.expertise_in(query.domain) -= 0.01
        
        # 更新风格偏好
        if feedback.prefers("detailed"):
            user.preference["detail_level"] = "comprehensive"
        elif feedback.prefers("concise"):
            user.preference["detail_level"] = "concise"
        
        # 更新 CoT 偏好
        if feedback.shows_interest_in("reasoning_steps"):
            user.preference["cot_detail"] = "maximal"
        
        self.user_store.update(user)
```

---

## 🎨 工作流程示意

### 完整决策流程

```
用户查询
    │
    ▼
┌─────────────────┐
│ 1. 意图识别层    │
│ - 语义分类       │
│ - 复杂度评估     │
│ - CoT 触发判断   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 2. 用户画像层    │
│ - 专业度匹配     │
│ - 风格偏好读取   │
│ - 历史行为分析   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 3. 策略选择层    │
│ - 基础策略模板   │
│ - 个性化调整     │
│ - 上下文适配     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 4. 并行检索层    │
│ - 向量检索       │
│ - 图谱多跳       │
│ - HyDE 增强      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 5. 结果融合层    │
│ - 去重           │
│ - 评分归一化     │
│ - 多样性保证     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 6. 重排序层      │
│ - BGE-Reranker  │
│ - Novelty 检测   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 7. 早停判断      │
│ - 置信度检查     │
│ - 延迟预算检查   │
│ - 满意度预测     │
└────────┬────────┘
         │
    ┌────┴────┐
    │  是     │  否
    ▼         ▼
  返回结果  继续检索
    │
    ▼
┌─────────────────┐
│ 8. 响应生成层    │
│ - 个性化风格     │
│ - CoT 展示       │
│ - 格式适配       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 9. 反馈收集      │
│ - 显式评分       │
│ - 隐式行为       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 10. 策略更新     │
│ - 权重调整       │
│ - 画像更新       │
│ - 模型优化       │
└─────────────────┘
```

---

## 📊 性能指标

### 预期提升效果

| 指标 | 基线 (传统 RAG) | NecoRAG 目标 | 提升幅度 |
|------|----------------|-------------|---------|
| **用户满意度** | 3.5/5.0 | 4.5/5.0 | +28.6% |
| **平均响应延迟** | 1200ms | 800ms | -33.3% |
| **检索命中率** | 65% | 80% | +23.1% |
| **资源利用率** | 低 (全量检索) | 高 (按需检索) | -40% 计算成本 |
| **新用户留存率** | 45% | 65% | +44.4% |
| **专家用户留存率** | 60% | 80% | +33.3% |

### 资源优化效果

通过早停和降级机制:
- **简单问题**: 仅执行 1 个策略，延迟从 800ms 降至 300ms
- **中等问题**: 执行 2-3 个策略，延迟从 1200ms 降至 600ms
- **复杂问题**: 执行全部策略，但通过并行化保持延迟<1000ms

---

## 🔗 与其他模块的协同

### 与意图分析系统的协同

```
意图分析系统
    │
    ├──▶ 7 大类意图分类 ──▶ 初始策略路由
    ├──▶ 复杂度评估 ──▶ CoT 触发判断
    └──▶ 置信度评分 ──▶ 早停机制输入
```

### 与 CoT 思维链的协同

```
CoT 思维链推理
    │
    ├──▶ 触发条件判断 ──▶ 是否启动 CoT
    ├──▶ 深度分级 ──▶ L1-L4 可调节
    └──▶ 可视化输出 ──▶ 个性化展示
```

### 与用户画像的协同

```
用户画像 (Personal Workspace)
    │
    ├──▶ 专业度评估 ──▶ 响应风格调整
    ├──▶ 风格偏好 ──▶ 格式和语调调整
    └──▶ 历史行为 ──▶ 策略权重更新
```

### 与巩固层的协同

```
Refinement Agent
    │
    ├──▶ 反馈收集 ──▶ 策略优化输入
    ├──▶ 质量评估 ──▶ 满意度预测模型
    └──▶ 知识固化 ──▶ 最优策略模板沉淀
```

---

## 💡 应用场景示例

### 场景 1: 技术方案选型 (推理演绎类意图)

**用户:** 中级开发者，专业度 0.6

**查询:** "我们应该选择微服务还是单体架构？"

**智能路由决策:**
1. **意图识别**: 推理演绎类 (置信度 0.85) → 触发 CoT
2. **用户画像**: 中级开发者 → L2 标准版 CoT(3-4 步)
3. **策略选择**: 
   - 图谱多跳检索 (0.4): 查找两种架构的特征和案例
   - HyDE 增强 (0.3): 生成假设性对比分析
   - CoT 推理 (0.3): 构建逻辑推理链条
4. **响应生成**: 
   - 平衡解释型风格
   - 完整展示推理过程
   - 提供技术术语解释

### 场景 2: 简单事实查询 (事实查询类意图)

**用户:** 专家级工程师，专业度 0.9

**查询:** "Python 3.12 的发布时间？"

**智能路由决策:**
1. **意图识别**: 事实查询类 (置信度 0.95) → 不触发 CoT
2. **用户画像**: 专家 → 简洁专业型
3. **策略选择**: 
   - 精确向量匹配 (0.7)
   - 关键字检索 (0.3)
   - 早停机制：置信度>0.95，立即返回
4. **响应生成**: 
   - 简洁回答:"2023 年 10 月 2 日"
   - 附带官方文档链接

### 场景 3: 跨领域探索 (探索发散类意图)

**用户:** 新手产品经理，专业度 0.3

**查询:** "区块链如何应用于供应链管理？"

**智能路由决策:**
1. **意图识别**: 探索发散类 (置信度 0.75) → 触发 CoT(L3 详细版)
2. **用户画像**: 新手 → L3 详细版 CoT(5-7 步)，通俗易懂
3. **策略选择**: 
   - 扩散激活 (0.4): 从区块链特征扩散到供应链痛点
   - 新颖性优先 (0.4): 寻找创新应用案例
   - 跨领域联想 (0.2): 连接两个领域的知识点
4. **响应生成**: 
   - 详细引导型风格
   - 配合类比和图示
   - 展示完整推理路径

---

## 🚀 实施路线图

### Phase 1: 基础框架搭建 (已完成)

- ✅ 意图分类系统与策略模板映射
- ✅ 用户画像数据结构设计
- ✅ CoT 触发判断逻辑实现

### Phase 2: 策略融合引擎开发 (进行中)

- 🔲 多策略并行检索框架
- 🔲 结果融合与重排序算法
- 🔲 早停与降级机制实现

### Phase 3: 反馈闭环建设 (计划中)

- 🔲 显式和隐式反馈收集
- 🔲 在线学习算法实现
- 🔲 用户画像动态更新

### Phase 4: 性能优化与调优 (计划中)

- 🔲 A/B 测试框架集成
- 🔲 超参数自动调优
- 🔲 性能监控与告警

---

## 📈 量化指标与监控

### 实时监控指标

1. **策略效果指标**:
   - 各策略的命中率和用户满意度
   - 策略组合的最优权重演变
   - 早停触发率和平均节省时间

2. **用户体验指标**:
   - 不同专业度用户的满意度分布
   - 响应风格偏好的使用统计
   - CoT 展示的点击率和完读率

3. **系统性能指标**:
   - 各检索策略的延迟分布
   - 并行检索的资源利用率
   - 降级机制触发频率

### 预警阈值

| 指标 | 正常范围 | 预警阈值 | 处理措施 |
|------|---------|---------|---------|
| 平均响应延迟 | <800ms | >1000ms | 自动降级 |
| 用户满意度 | >4.0 | <3.5 | 策略调整 |
| 早停触发率 | 20-40% | <10% 或 >60% | 参数调优 |
| CoT 触发率 | 30-50% | <20% 或 >70% | 阈值校准 |

---

## 🎓 技术优势总结

### 创新性

1. **三层决策架构**: 业界首个整合意图、画像和策略的 RAG 路由系统
2. **动态 CoT 调节**: 根据用户专业度自适应调整推理深度
3. **在线学习闭环**: 实时反馈驱动的策略自优化

### 实用性

1. **显著降低延迟**: 早停机制平均节省 40% 计算时间
2. **提升用户体验**: 个性化适配使满意度提升 28.6%
3. **资源高效利用**: 按需检索降低 40% 计算成本

### 可扩展性

1. **策略插件化**: 支持第三方开发者贡献新策略
2. **意图可扩展**: 预留新的意图类型接口
3. **画像可定制**: 支持企业自定义画像维度

---

## 📚 相关文档

- [design.md](../design/design.md) - NecoRAG 整体技术框架
- [意图分析系统](../docs/wiki/意图分析系统.md) - 详细的意图分类设计
- [CoT 思维链](./COT_CHAIN_OF_THOUGHT.md) - CoT 推理机制详解
- [用户画像](../docs/wiki/记忆管理层/用户画像与个性化.md) - 个人工作空间与用户画像设计

---

**文档版本:** v3.3  
**创建日期:** 2026-03-19  
**最后更新:** 2026-03-19  
**作者:** NecoRAG Team  
**状态:** ✅ 设计完成，待实施
